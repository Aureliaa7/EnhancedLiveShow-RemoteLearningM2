@model LiveShow.Services.Models.User.UserChangePasswordDto

@{ ViewData["Title"] = "Edit account";
    if (User.IsInRole("Artist"))
    {
        Layout = "~/Views/Shared/_LayoutArtist.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_LayoutUser.cshtml";
    }
}

<h4 style="text-align: center; margin-top: 70px; color: azure;">Change password</h4>

<hr />
<div class="row justify-content-center" style="margin-top:40px;">
    <div class="jumbotron">
        <form asp-action="ChangePassword">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CurrentPassword" class="control-label"></label>
                <input asp-for="CurrentPassword" class="form-control" />
                <span asp-validation-for="CurrentPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NewPassword" class="control-label"></label>
                <input asp-for="NewPassword" class="form-control" />
                <span asp-validation-for="NewPassword" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" id="button-form"/>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
